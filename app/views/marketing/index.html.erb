<h1>Multi-tenant SaaS</h1>
<p>This is a placeholder link for local dev — in production you wouldn't
   know a visitor's tenant from the marketing site, so this would go to
   a "find your workspace" or plan-selection flow instead.</p>
<p><%= link_to "Sign in (acme demo tenant)", new_session_path(subdomain: "acme") %></p>
